# liulian-python — 2026-06

Live ledger. Topic tags: `[id-exp]` entity-identifier experiments ·
`[optim]` HPO / search spaces · `[data]` data layer · `[infra]` CI /
branches / cluster · `[results]` reporting & artifacts. Agent session task
IDs (`#N`) kept for traceability.

## In progress

- `[id-exp]` **swiss3dt — onehot running, dim modes queued** (#30) —
  run-tags `swiss3dt-*-20260612`, dropout tuned `{0, 0.1}`, seeded trials.
  DONE: none + embedding (see Completed). RUNNING: onehot (5875871-73).
  QUEUED: random (5878233-35) + sinusoidal+coordinates (5878236-38) —
  these start under the NEW wrapper path (id_injection='model', commit
  `8611acd`) with genuinely tuned dims; no resubmission was needed (the
  sbatch scripts only carry CLI args; behavior comes from the synced
  repo/config).

## Future

- `[id-exp]` **Nowcast decoding scheme** (#31) — per-step decoding head
  for `lstm.py` under `task='nowcast'` (benchmark `LstmModel` style: head
  on every LSTM output, full-sequence masked loss); audit other adapters
  (DMS models may need a `pred_len=seq_len` mode); optional per-step
  auxiliary loss for forecast. Details:
  `liulian-python/docs/research/entity-id-deep/lstm.md` §9 + inline
  `TODO(nowcast)` in `liulian/models/torch/lstm.py`.
  **Do BEFORE any nowcast experiment.**
- `[id-exp]` **multi_channel coordinates injection** (#28) — pipeline
  never injects `config['coordinates']` / `config['station_ids']` for
  `ChannelTransparentWrapper`, so coordinates are offered only for
  swiss × lstm (per_entity). Wire the injection, re-offer the
  patchtst/dlinear pairs, rerun the invalidated swiss-river-1990 cells.
  Needs a NaN-masking story before any 2010/zurich multi_channel run.
- `[results]` **Invalidate pre-2026-06-11 swiss coordinates results in
  research docs** (#29) — those cells ran zero-vector identifiers; mark
  them invalid in the 2026-05-15 slide / analysis / heatmaps and replace
  with swiss3 reruns.
- `[id-exp]` **Case 2: outer-sweep param-analysis entry point** (#26) —
  sweep a transparent dim (e.g. `sinusoidal_dim`) while FIXING inner HPO
  to the swept value; the only way to study these dims on per_entity
  swiss-lstm (features frozen into the loaders per run). Separate entry
  point, not the matrix runner.
- `[id-exp]` **sin/random identifier backfill for traffic + electricity**
  (#17) — deferred cells of the 2026-05 baseline matrix.
- `[infra]` **`hpo_local_mode` cleanup** — current Ray removed
  `local_mode`; the config knob is dead and ~56 e2e tests fail on it
  (`ray_optimizer.py:586`). Migrate the tests, drop the knob.
- `[data]` **trainer.py predict-aggregate bug** — `TypeError` at
  `trainer.py:623` (`sum(all_entity_ids, [])` with tuples); pre-existing,
  one e2e test fails. One-liner once approved.
- `[results]` **results.json `model.*` shows pre-HPO defaults** —
  cosmetic: the model section reflects the initial config; tuned values
  are in `hpo.best_hparams` / `best_hparams.yaml`. Align when convenient.
- `[infra]` **Repo hygiene** — stray `ls` file at repo root; root-level
  demo screenshots; decide what stays.

## Completed

### 2026-06-12

- `[id-exp]` **swiss3dt none + embedding completed** — test denorm RMSE
  (°C): 1990 none 1.723 → emb **1.289** (−25%); 2010 none 1.642 → emb
  **1.368** (−17%); zurich none 1.553 → emb **1.378** (−11%). Tuned
  embedding_size 16/9/19; dropout picked 0.0 in 4/6 cells, 0.1 in 2/6 —
  tuning it (vs hardcoding) was justified.
- `[optim]` **Transparent identifiers unified behind
  EntityTransparentWrapper** (`8611acd`, supersedes the loaders_factory
  path as canonical) — per_entity transparent modes now inject at the
  MODEL layer like embedding/multi_channel; wrapper rebuilt per HPO trial
  with the sampled dim (inner model rebuilt at enc_in = base + dim).
  `id_injection: model|data` knob; 15-case bitwise-equivalence suite.
  Artifact-level verification: every trial checkpoint's input width ==
  1 + sampled dim ({3,14,16,17}).
- `[data]` **Latent super().__init__ overwrite bug fixed** —
  SwissRiverDataset silently reset sinusoidal_dim / random_identifier_dim
  / random_identifier_seed (and the new id_injection) to parent defaults;
  custom dims NEVER reached the data path. This also invalidated the
  first (v1) loaders_factory verification — trial dir names varied while
  all trials trained on dim-16 data, masked by the dynamic enc_in peek.
  Lesson recorded: verify trial ARTIFACTS (checkpoint weight shapes),
  not run labels.

- `[optim]` **Data-layer dims genuinely tunable on per_entity**
  (`6f35a4a`) — make_trainable gains `loaders_factory`: trials sampling
  sinusoidal_dim / random_identifier_dim rebuild dataset+loaders inside
  the Ray worker and re-sync enc_in; post-HPO the final evaluation
  rebuilds loaders with the winning dim. Verified by a real 4-trial HPO
  (zurich+sinusoidal): dims sampled {3, 14, 16, 17}, best=3, finite
  final metrics. Supersedes the earlier per_entity gating decision.

- `[results]` **results.json `hpo.best_hparams` populated** — producer
  alias + builder fallback + regression tests
  (`tests/runtime/test_results_json.py`); the three completed swiss3
  none runs backfilled from `best_hparams.yaml` (commit `b3acc80`).
- `[optim]` **Per-trial Ray seeding** — `_trainable` seeds
  `seed + trial_index`; trials reproducible, differing only through
  sampled hyperparameters (commit `edec5d0`). Neither reference project
  (TSL, swiss benchmark) seeds trials; neither does CV — both select on
  a single fixed chronological hold-out, as we do.
- `[optim]` **Dropout for swiss lstm: tuned, not assumed** — reference
  `LstmModel` has no dropout; ours applied 0.1 silently. Now HPO chooses
  `{0, 0.1}`; config default 0.0.
- `[id-exp]` **LSTM decoding-scheme comment corrected** (`128fe03`) —
  ours is direct multi-step (TSL contract; = benchmark
  `ExtrapoLstmModel('limo')`); the benchmark's plain `LstmModel` decodes
  per time step and is NOT truncated; produce-then-truncate is TSL's exp
  loop (`outputs[:, -pred_len:]`).
- `[infra]` **Branch reorg** — `feat/platform-upgrade-2026-05` merged to
  `main` (merge `a3dc2f9`) and deleted; new `feat/update-2026-06`
  (platform) and `exp/entity-identifier-2026-06` (current working
  branch).
- `[infra]` **CI lint fix** — three E501 over-long comment lines wrapped
  (cherry-picked to main).

### 2026-06-11

- `[id-exp]` **swiss3 none baselines on UBELIX (gratis)** — verified
  genuine (50 trials, 30 epochs, batch 32 fixed, no caps). Test denorm
  RMSE: 1990 = 1.669 °C, 2010 = 1.646 °C, zurich = 1.562 °C. Run-tags
  `swiss3-*-20260611` (kept for old-vs-new dropout comparison).
- `[data]` **Coordinates identifier fixed** (`421f1e7`) — was silently
  fake: with `graph_mode='none'` the topology never loaded and
  `make_entity_features` zero-filled → every pre-fix swiss coordinates
  cell measured "2 constant zero channels". Now: topology loads for
  coordinates mode, coords min-max normalized per dataset, missing
  coords RAISE.
- `[data]` **Per-station NaN drop** (`421f1e7`) — swiss-river-2010 /
  zurich CSVs carry NaNs (stations join/leave the network); a single NaN
  sample drove the per_entity loss to NaN from epoch 1. Dropped per
  station; segment breaks keep windows from spanning gaps; no-op for
  1990.
- `[id-exp]` **swiss-river-2010 / zurich registered** in the matrix;
  per-experiment launcher `experiments/entity_identifier/run_job.py`;
  swiss lstm `batch_size` 8 → 32 (fixed, not HPO-tuned) (`e851ca6`).
- `[optim]` **HPO search spaces externalized** to
  `liulian/optim/search_spaces.yaml` (mode-aware identifier params;
  front-end editable). Fixed the dead-knob bug (lstm always tuned
  `embedding_size` even for `none`); per-trial
  `ChannelTransparentWrapper` rebuild makes sinusoidal/random dims real
  knobs in multi_channel (`ccba987`).
